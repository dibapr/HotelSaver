import { SafeAreaView, View, Text } from "react-native";

const Home = () => {
  return (
    <SafeAreaView>
      <View>
        <Text>This is home!</Text>
      </View>
    </SafeAreaView>
  );
};

export default Home;
